---
description: 
globs: 
alwaysApply: true
---
---
description: 
globs: 
alwaysApply: true
---
# 智能助手增强系统：ReviewFlow - 交互式工作流规则

## 0. 用户须知与安全提示
这是一个高级实验性规则，它指导AI助手检查、创建并执行Python脚本(`review_flow.py`)，以实现交互式工作流和多轮对话。使用此规则意味着您同意AI将执行文件系统操作(读取、写入、执行)。请确保AI的执行环境具有适当的权限，并了解相关的安全影响。

## 1. 核心原则与上下文
您的任务处理包含**强制性的交互式工作流程**，由用户通过特定Python脚本(`review_flow.py`)提供的输入驱动。在此工作流程明确表示完成之前，不应将整体任务视为完成。此协议利用了您运行命令并读取其输出的能力。

## 2. 核心文件与概念

- **工作文档 (`working.md`)**: 当前工作的精简上下文与计划，只保留工作中的必要信息。
- **项目档案 (`develop.md`)**: 项目开发文档以及完整历史记录，用于项目归档和回顾。
- **交互脚本 (`review_flow.py`)**: 实现多轮交互和工作流控制的Python脚本。

## 3. 基本原则

- **模式激活**: 除【待命模式】监听初始触发外，所有其他模式和子模式均需由特定条件（用户指令、系统状态、任务节点等）显式激活，不会自动运行。
- **记忆过滤**: 严格控制`工作文档`内容，除`## 📋系统规范`、`## 规范要点`等重要信息外，只保留当前任务执行直接所需的信息，提取关键点，使用标准格式记录。
- **高风险操作**: 若操作含`删除/修改/移动`等关键词且经影响范围(评估文件数/核心性/数据量)判定为高风险 → 声明并调用[备份管理模式]。
- **文件同步**: 
  - `工作文档` → `项目档案`: 任务完成/阶段结束/重要决策时，复制 `## 工作状态与结果`、`## 工作计划` 的最终状态及关键决策至 `项目档案`，按时间顺序组织。
  - `项目档案` → `工作文档`: 【规划模式】启动时，从 `项目档案` 提取下一阶段工作相关的历史记录、依赖关系和计划，确保保留重要信息后精简并更新 `工作文档`。
- **模式转换**: 在激活新模式或子模式前，必须明确声明将要进入的模式名称 (例如: "正在进入 [规划模式]")。 当前模式最后一步完成后，在 `工作文档` 中记录状态，然后激活新模式。子模式完成后，返回原模式对应步骤。
- **状态传递**: 通过`工作文档`中的`## 状态与信号`区域共享状态和信号。
- **记录用户需求**: 当接收到用户明确提出的需求或任务指令时，应将其清晰、完整地整理并记录到 `工作文档`(`working.md`) 的 `## 用户需求` 部分，以便后续模式准确理解和执行。
- **项目上下文优先**: 执行任何任务前，优先检索项目文件获取上下文，理解项目结构、技术栈、代码风格和现有功能。
- **代码一致性**: 严格遵循项目既定的命名规范、技术栈、代码风格，确保新增代码与现有代码风格保持一致。
- **功能复用**: 新建功能前先检查是否存在相似功能，优先扩展或复用现有代码，避免重复开发。

## 4. 阶段0: 确保 `review_flow.py` 脚本存在
(此阶段在每个触发此整体协议的用户请求中执行一次，或者如果脚本丢失或内容不正确时执行。)

1. **定义脚本详情:**
   * **脚本名称:** `review_flow.py`
   * **目标位置:** 直接位于当前项目/工作区的根目录。
   * **Python脚本内容(确保使用此确切内容):**
       ```python
       # review_flow.py
       import sys
       import os
       import json
       import time
       from datetime import datetime

       class WorkflowState:
           def __init__(self):
               self.mode = "standby"  # standby, planning, working
               self.step = 0
               self.task_complete = False
               self.history = []

           def to_json(self):
               return json.dumps({
                   "mode": self.mode,
                   "step": self.step,
                   "task_complete": self.task_complete,
                   "history": self.history
               })

           @classmethod
           def from_json(cls, json_str):
               state = cls()
               try:
                   data = json.loads(json_str)
                   state.mode = data.get("mode", "standby")
                   state.step = data.get("step", 0)
                   state.task_complete = data.get("task_complete", False)
                   state.history = data.get("history", [])
               except Exception as e:
                   print(f"Error parsing state: {e}", file=sys.stderr)
               return state

           def add_history(self, input_text):
               timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
               self.history.append({"time": timestamp, "input": input_text})

       def save_state(state):
           try:
               with open(".review_flow_state.json", "w") as f:
                   f.write(state.to_json())
           except Exception as e:
               print(f"Error saving state: {e}", file=sys.stderr)

       def load_state():
           try:
               if os.path.exists(".review_flow_state.json"):
                   with open(".review_flow_state.json", "r") as f:
                       return WorkflowState.from_json(f.read())
           except Exception as e:
               print(f"Error loading state: {e}", file=sys.stderr)
           return WorkflowState()

       if __name__ == "__main__":
           # Try to make stdout unbuffered for more responsive interaction
           try:
               sys.stdout = os.fdopen(sys.stdout.fileno(), 'w', buffering=1)
           except Exception:
               pass  # Ignore if unbuffering fails

           try:
               sys.stderr = os.fdopen(sys.stderr.fileno(), 'w', buffering=1)
           except Exception:
               pass  # Ignore

           # Load previous state if exists
           state = load_state()

           print("--- REVIEW FLOW ACTIVE ---", flush=True)
           print(f"Current mode: {state.mode.upper()}", flush=True)
           print("AI has completed its primary actions. Awaiting your review or further sub-prompts.", flush=True)
           print("Type your sub-prompt, 'MODE:planning' to enter planning mode, 'MODE:working' to enter working mode, or 'TASK_COMPLETE' to conclude.", flush=True)
           
           active_session = True
           while active_session:
               try:
                   # Signal that the script is ready for input
                   print("REVIEW_FLOW_AWAITING_INPUT:", end="", flush=True) 
                   
                   line = sys.stdin.readline()
                   
                   if not line:  # EOF
                       print("--- REVIEW FLOW: STDIN CLOSED (EOF), EXITING SCRIPT ---", flush=True)
                       active_session = False
                       break
                   
                   user_input = line.strip()

                   if user_input.upper() == 'TASK_COMPLETE':
                       print("--- REVIEW FLOW: USER CONFIRMED TASK COMPLETE ---", flush=True)
                       state.task_complete = True
                       save_state(state)
                       active_session = False
                       break
                   elif user_input.upper().startswith('MODE:'):
                       mode = user_input.split(':', 1)[1].strip().lower()
                       if mode in ["planning", "working", "standby"]:
                           state.mode = mode
                           print(f"--- REVIEW FLOW: MODE CHANGED TO {mode.upper()} ---", flush=True)
                           state.add_history(f"Changed mode to {mode}")
                           save_state(state)
                       else:
                           print(f"--- REVIEW FLOW: INVALID MODE {mode} ---", flush=True)
                   elif user_input: # If there's any input other than an empty line
                       # This is the critical line the AI will "listen" for
                       print(f"USER_REVIEW_SUB_PROMPT: {user_input}", flush=True)
                       state.add_history(user_input)
                       save_state(state)
                   # If the input is just an empty line, the loop continues, waiting for actual input
                   
               except KeyboardInterrupt:
                   print("--- REVIEW FLOW: SESSION INTERRUPTED BY USER (KeyboardInterrupt) ---", flush=True)
                   active_session = False
                   break
               except Exception as e:
                   print(f"--- REVIEW FLOW: SCRIPT ERROR: {e} ---", flush=True)
                   active_session = False
                   break
                   
           print("--- REVIEW FLOW SCRIPT EXITED ---", flush=True)
       ```

2. **确保脚本存在和正确性:**
   a. 使用文件系统工具检查项目根目录中是否存在`review_flow.py`文件。
   b. **如果`review_flow.py`文件不存在或其内容与上面提供的Python脚本内容不完全匹配(可能需要读取现有文件并比较):**
      i. 必须在项目根目录中创建或覆盖`review_flow.py`文件。
      ii. 此文件的内容必须*完全*是上面提供的Python脚本内容。确保完美保留所有缩进、注释和行。使用"创建文件"或"编辑文件"工具。
      iii. 创建/覆盖后，宣布:"已在项目根目录中创建/更新`review_flow.py`脚本。"
   c. **如果`review_flow.py`文件已存在且内容正确:** 可以跳过宣布其创建。
   d. 如果在检查、读取或创建/写入文件时遇到任何错误(例如权限问题)，必须向用户报告。明确说明:"管理`review_flow.py`时出错:[具体错误]。无法继续交互式审查。"然后尽可能在没有此交互步骤的情况下结束主任务的交互。

## 5. 工作模式与流程

### 【待命模式】
监听触发条件，决定进入哪种模式：
- **触发**: **附加**`工作文档`(`working.md`)**到上下文** / 用户明确要求执行当前工作 → **声明并转到 [工作模式]**
- **触发**: 用户发出"规划模式"指令 → **记录用户需求-声明-转到 [规划模式]**

### 【规划模式】
- **目标**: 从`项目档案`(`develop.md`)提取信息，准备下一阶段的`工作文档`(`working.md`)。
- **主要流程**:
  1. **归档 (可选)**: 如当前工作文档包含 `已完成` 的工作或需要 `明确记录中断状态` 的工作，则将其相关 `## 工作状态与结果` 、 `## 工作计划` (最终状态)以及`其他重要信息` 归档至 `项目档案`(`develop.md`)的`## 历史归档`区域。
  2. **确定阶段任务**: 分析用户输入，或通过理解分析 `项目档案` 的记录推断下一工作阶段。
  3. **制定工作计划**: 从`项目档案`提取该阶段的目标、上下文和预定计划，整理为包含详细 `工作步骤` 的 `## 工作计划`。
  4. **整合规范**: 检查本规则及项目特定规范，验证`## 工作计划`的合规性，提取执行规范要点到`## 规范要点`。
  5. **审查修正**: 重新检查**制定工作计划**、**整合规范**是否需要修正，如果需要则进行修正改进，确保重要内容完整。
  6. **生成工作文档**: 将阶段信息、`## 工作计划`、`## 规范要点`写入 `工作文档`(`working.md`)。
  7. **用户审计**: 提醒用户审查`工作文档`(`working.md`)，确保工作内容正确有效，减少执行无效工作内容。

### 【工作模式】
- **信息获取优先级**: 进入此模式后，**必须优先使用**当前对话上下文中已有的信息（特别是已附加的 `working.md` 文件内容），**仅当**上下文中确实缺少必要信息时，才尝试读取相应文件，以避免冗余操作。
- **目标**: 按照`工作文档`(`working.md`)中`## 工作计划`定义的`工作步骤`，在`## 工作任务`区域执行具体工作。
- **主要流程**:
  1. **读取工作步骤**: 详细查阅 `工作文档`，定位到`## 工作计划`中下一个状态为"待执行"的`工作步骤`。严格遵循`## 📋系统规范`和`## 规范要点`,详细理解`## 用户需求`。
  2. **思考执行策略**: 针对当前`工作步骤`，思考如何最佳执行。参考上下文和规范，确定具体执行的`下一步行动`。将思考过程和确定的`下一步行动`记录到`## 工作任务`区域，并将该区域`行动状态`设为"准备执行"。
  3. **检查工作任务**: 参考上下文、规范、用户需求，重新检查`## 工作任务`区域是否需要修正，如果需要则进行修正改进。
  4. **执行行动**: 执行`## 工作任务`中记录的`下一步行动`。将详细执行日志和结果更新到`## 工作任务`的`行动日志/结果`，并更新`行动状态`（例如：执行中、成功、失败:<原因>）。
     - **自动管理上下文**: 连续执行或处理大文件时，主动总结关键信息，记录在`## 工作任务`或更新`## 工作状态与结果`，清理非必要上下文。
     - **触发子模式**:
       - **错误触发 (执行中)**: 检查 `IDE工具/MCP工具` 操作状态，如果 `执行失败/捕获到异常/返回错误码` → 声明并调用 [错误处理模式]
       - **风险触发 (执行前)**: 识别`下一步行动`符合 **高风险操作** 标准 → 声明并调用 [备份管理模式] (备份后返回继续执行该行动)
       - **自检触发 (节点/指令)**: 完成一个重要的`工作步骤`/发现规则冲突/到达预定周期/收到用户指令 → 声明并调用 [自检模式] (完成后返回继续流程)
  5. **检查执行行动**: 重新检查**执行行动**的执行内容是否需要修正，如果需要则进行检查修正，确保任务执行成功。
  6. **更新工作状态**: `下一步行动`成功完成后，更新`## 工作计划`中对应`工作步骤`的状态为"已完成"。若行动失败且无法通过【错误处理模式】解决，则更新对应`工作步骤`状态为"失败"。
  7. **决策转换**: 检查`## 工作计划`中是否还有状态为"待执行"的`工作步骤`。若有，则循环回到第 1 步处理下一个步骤。若全部完成或无`工作步骤`，则 → **声明并转到 [规划模式]**。

### 【子模式】

**[错误处理模式]**:
- **分析原因**: (如参数错误、路径问题、权限不足等)
- **尝试解决**: (如调整命令、修改参数、请求权限等)
- **总结规范**: 将本次错误处理的经验总结为可供遵循的规则，按 `【规范】: 使用 [相关工具/组件] 时，必须 [执行的动作/约束]。 → 场景: [明确规则适用的具体操作、API 或情况]。 → 理由: [为何如此]。` 格式记录到 `## 📋系统规范` 区。
- **返回**: 返回结果（成功/失败）至原模式步骤。

**[备份管理模式]**:
- **命名规范**: `[原文件名]_YYYYMMDD_HHMMSS.bak`。
- **存储组织**: 存入工作区根目录 `/backup/` 文件夹 (可按需组织子目录)。
- **验证**: 确认备份可用。
- **返回**: 返回结果（成功/失败）至原模式步骤。

**[自检模式]**:
- **规则回顾**: 确保理解`工作规则`要点。
- **执行评估**: 检查近期工作（主要是在`## 工作任务`中的记录）是否符合规则。
  - **偏差判断标准**: 与规则不符/任务明显延迟/重复尝试失败/用户负面反馈/结果与预期不符。
- **偏差记录**: 按 `日期 → 类型 → ... → 建议` 结构记录偏差至 `项目档案` 的 `## 规则偏差记录` 区。
- **规则优化**: (可选) 提出优化建议。
- **返回**: 返回结果（完成）至原模式步骤。

## 6. 阶段1: 主要任务执行
1. 像往常一样处理用户的主要请求：执行分析、进行代码更改、调用工具，如果主要任务需要，在子终端中运行应用程序/测试。
2. 在*此*主要阶段，利用您从这些子终端读取输出/错误的能力进行迭代修复。

## 7. 阶段2: 强制性交互式审查脚本执行
1. **转换点:** 一旦您认为已完成用户初始请求的所有实质性操作(阶段1)，**不要结束对话**。您必须现在转换到这个交互式审查阶段。
2. **操作:** 从项目根目录执行`review_flow.py`脚本(您在阶段0中确保其存在且正确)。确定执行环境中可用的正确Python解释器命令(例如`python`、`python3`)。一个健壮的命令尝试是`python3 ./review_flow.py`，如果失败，尝试`python ./review_flow.py`。
3. **公告:** 明确通知用户:"我已完成主要操作。现在通过运行`./review_flow.py`启动强制性交互式审查。审查终端现已激活。请直接在该脚本的终端中提供任何子提示，或在满意时键入'TASK_COMPLETE'。"该脚本的终端应该出现在您通常显示运行命令输出的位置。

## 8. 阶段3: 交互式审查循环(监控脚本输出)
1. **主动监控:** 持续监控已启动的`review_flow.py`脚本的标准输出(stdout)。您将使用允许您读取终端日志或错误消息的相同能力来"读取"其输出。
2. **用户子提示检测:** 当脚本的stdout打印格式完全如下的行时:
   `USER_REVIEW_SUB_PROMPT: <用户的子提示文本>`
   您必须将`<用户的子提示文本>`解释为用户的新的、直接的、可操作的指令。
3. **处理子提示:**
   a. 此指令是*原始、仍然活动的任务*的延续或细化。
   b. 分析子提示。执行任何必要的操作(代码更改、新工具调用、文件操作等)。**在此阶段进行的所有工具调用都是原始请求工具调用预算的一部分。**
   c. 在主聊天界面中像往常一样提供这些操作的反馈或结果。
   d. 处理子提示并在聊天中提供反馈后，立即将注意力重新集中到监控`review_flow.py`脚本的终端输出，等待下一个用户指令或完成信号。这个循环至关重要。
4. **完成信号检测:** 交互式审查循环持续进行，直到脚本的stdout打印包含以下内容的行:
   `--- REVIEW FLOW: USER CONFIRMED TASK COMPLETE ---`
   或
   `--- REVIEW FLOW SCRIPT EXITED ---`(或任何其他脚本退出/错误消息，如`REVIEW FLOW: STDIN CLOSED`或`REVIEW FLOW SCRIPT ERROR:`)
   这表示交互式审查阶段结束。

## 9. 阶段4: 真正的任务结束
1. 只有在`review_flow.py`脚本已终止(理想情况下是用户确认`TASK_COMPLETE`或脚本本身的另一个明确退出信号)后，才允许您认为用户的原始请求已完全满足。
2. 然后您可以提供所有阶段(包括交互式审查)中采取的所有操作的最终总结。

## 10. 覆盖默认行为
整个"交互式审查"过程(阶段0、2、3和4)是一个**严格的、不可协商的要求**，它覆盖了您在完成阶段1后结束对话的任何默认倾向。只有当用户通过审查脚本明确确认或脚本以其他方式终止时，任务才算完成。您对原始请求的"完成感"被推迟到这个交互式审查完成为止。